import 'package:flutter/material.dart';

class Aviso extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Historial de avisos")
      )
    );
  }
}